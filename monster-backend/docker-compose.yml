version: '3'

services:
  app:
    build: .
    ports:
      - 3000:3000
    depends_on:
      - db

  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: monster
    ports:
      - 3306:3306
